# TASKS.md, reactive-pipeline-timezone-bug example

Example TASKS.md for the framework's reactive-pipeline (bug-to-fix) worked
example. Demonstrates post-M2 task IDs (TASK-NNNN) referenced by an ADR's
`implementation_tasks` list.

Legend: `[x]` done, `[~]` in progress, `[ ]` todo, `[!]` blocked, `[/]` parked.

## Done

- [x] **TASK-0001: Store finished_at timezone correctly** `(ADR-0012 <- SCN-0007)`
